apiVersion: v1
kind: Service
metadata:
    name: "customerportalmvc-4dc4"
    labels:
        app: "customerportalmvc-4dc4"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "customerportalmvc-4dc4"
